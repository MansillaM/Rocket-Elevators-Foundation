<%= form_with(model: intervention, local: true) do |f| %>

  <% if intervention.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(intervention.errors.count, "error") %> prohibited this intervention from being saved:</h2>

      <ul>
      <% intervention.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<center> 

  <h1>
    <bold> 
        INTERVENTION TICKET
    </bold>
  </h1> 

  <div>
      <%= f.label :customer_id, "Select Customer" %> 
      <%= f.collection_select :customer_id, Customer.order(:name), :id, :name %> 
  </div>

  <br>

  <div>
      <select name="intervention[building_id]" id="intervention_building_id">
      </select>
  </div>

  <br>

  <div>
      <select name="intervention[battery_id]" id="intervention_battery_id">
      </select>
  </div>

  <br>

  <div>
      <select name="intervention[column_id]" id="intervention_column_id">
      </select>
  </div>

  <br>

  <div>
      <select name="intervention[elevator_id]" id="intervention_elevator_id">
      </select>
  </div>

  <div class="allEmployees">
      <%= f.label :employee_id, "Top Employees" %> 
      <%= f.collection_select :employee_id, Employee.order(:id), :id, :title %> 
  </div>

  <div class="longMessage">
      <%= f.label :report, "Description" %>
      <%= f.text_area :report, :cols => 50, :rows => 10, :style => 'resize: none'%>
  </div>

  <div class="actions">
      <%= f.submit %>
  </div>
</center>
<% end %>

